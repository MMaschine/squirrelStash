+++ Property card should be smaller with horizontal placements of boxes, not vertical
+++ Check validation of properties. Now it is possible to create numeric ones and empty "Complex"
+++ Hide filter for now, turn them into grouping next 
+++ CAtegory title in create form is redundant 
+++ Change items number works wrongly 
+++ Rework Tree view: One component with Card search and Button to add 
+++ Add search logic
+++ Add number of items on the Items tab
+++ Remove "category title"
+++ Fix id problem for new items 
+++ V> in small black round
+++ Messages to resources
+++ If we just added Item, the list should be opened 
+++ CAtegory name Existance validation
+++ Bug: not allow numeric values for the numeric type 
+++ Bug: delete property icon is strange 
+++ Default Quantity for item 
+++ Logs 
+++ Refactoring. Create service to deal with images, inject in dialog properly + factory for dialog 
+++ Add size for strings in data-objects 
+++ ShareFile integration for exceptions
+++ OrderBy
+++ Specific OrderBy for properties with predefined values (Allowed values) if requested
-----------------------------------------------------------------------------------------------------------------------

UI: 
- Cards visual rework 
- Lock "Apply" button in creation of Category if no properties 
- Change "hide" button
- Divide the properties in an Item name by "/"
- Total number of categories and items in overview is useless. Delete 
- OrderBy visible only if we have any items 
- Highlight item by border color if it is reach the threshold (Tree view)

Func: 
- Edit/Delete for Categories and Items both 
- Editable (dragable) order of Properties in Create Category Dialog
- "Copy item" function

Bug: 
- BUG: Add several properties. Edit. The screen is shaking when inputing. Especially visible with "Allowed values".
- BUG: Clean the Edit form in case of error 

Tst: 
- Test case when sorting involve property without value (newly added property)
-----------------------------------------------------------------------------------------------------------------------
Small possible improvements: 

